# Samar's IPO Market - Daily Report configuration
# Copy this file to `.env` and fill in YOUR OWN credentials (placeholders only here).

# ----- Email (SMTP) -----
USER_MAIL_TO=manishgamare@gmail.com
USER_SMTP_HOST=smtp.gmail.com
USER_SMTP_PORT=587
USER_SMTP_USER=your-email@gmail.com
USER_SMTP_PASSWORD=your-app-password

# ----- WhatsApp (Twilio / WhatsApp Business API) -----
USER_WHATSAPP_TO=+917397939279
USER_TWILIO_ACCOUNT_SID=your-twilio-account-sid
USER_TWILIO_AUTH_TOKEN=your-twilio-auth-token
USER_TWILIO_FROM=whatsapp:+14155238886

# Approved WhatsApp content template (Content SID, starts with HX...).
# Create one template whose body is a single variable {{1}}; the whole report
# is passed in as ContentVariables. Required for new Twilio accounts (error 21654).
USER_TWILIO_CONTENT_SID=
